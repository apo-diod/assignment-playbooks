---

- name: Deploy new mindmaps version
  hosts: 127.0.0.1
  remote_user: hostadmin

  tasks:
    - name: Check user
      debug:
        msg: "{{ ansible_user_id }}"
    - name: Check docker version
      ansible.builtin.shell:
        cmd: docker ps