---

- name: Deploy new mindmaps version
  hosts: localhost
  remote_user: hostadmin
  connection: local

  tasks:
    - name: Check user
      debug:
        msg: {{ ansible_user_id }}
    - name: Check docker version
      ansible.builtin.shell:
        cmd: docker version